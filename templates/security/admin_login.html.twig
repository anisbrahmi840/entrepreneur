{% extends 'base.html.twig' %}

{% block title %} Admin | Connecter {% endblock %}

{% block body %}
<section id="contact-us" class="contact-us section">
    <div class="container">
        <div class="row">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="section-title">
                    <h1>Se connecter</h1>
                </div>
            </div>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-md-6 col-sm-6 col-xs-12 header-inner">
                <form class="form" method="post">
                    {% if error %}
                    <div class="alert alert-danger">Vérifiez votre e-mail ou votre mot de passe</div>
                    {% endif %}
                    <div class="col-md-12">
                        <div class="form-group">
                            <input type="email" value="{{ last_username }}" name="email" id="inputEmail"
                                placeholder="Email" class="form-control" autofocus> </div>
                    </div>
                    <div class="col-md-12">
                        <div class="form-group">
                            <input type="password" name="password" id="inputPassword" class="form-control"
                                placeholder="mot de passe">
                        </div>
                    </div>
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                    <div class="col-md-12">
                        <div class="form-group text-center">
                            <button type="submit" class="btn primary">Connecter</button>
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
{% endblock %}