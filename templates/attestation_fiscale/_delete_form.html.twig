<form method="post" action="{{ path('attestation_fiscale_delete', {'id': attestation_fiscale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ attestation_fiscale.id) }}">
    <button class="btn">Delete</button>
</form>
