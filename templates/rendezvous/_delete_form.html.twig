<form method="post" action="{{ path('rendezvous_delete', {'slug': rendezvou.slug}) }}">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rendezvou.id) }}">
    <button class="fa fa-trash fa-lg text-danger"></button>
</form>
