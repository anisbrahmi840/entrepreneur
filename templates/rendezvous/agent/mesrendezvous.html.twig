{% extends 'admin.html.twig' %}

{% block title %}Rendez-vous index{% endblock %}

{% block body %}
<section id="contact-us" class="contact-us section">
    <div class="container">
        <div class="row text-center">
            <div class="col-md-12 col-sm-12 col-xs-12">
                <div class="section-title">
                    <h1>Mes rendez-vous</h1>
                </div>
            </div>
        </div>

        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Date de création</th>
                    <th>Date de rendez-vous</th>
                    <th>Entrepreneur</th>
                    <th>Objet</th>
                    <th>Etat</th>
                    <th>Observation</th>
                </tr>
            </thead>
            <tbody>
                {% for rendezvou in rendezvous | sort  %}
                <tr>
                    <td>{{ rendezvou.createdAt ? rendezvou.createdAt|date('d-m-Y H:i') : '' }}</td>
                    <td>{{ rendezvou.daterendezvous ? rendezvou.daterendezvous|date('d-m-Y H:i') : '' }}</td>
                    <td>{{ rendezvou.entrepreneur.nom }} {{ rendezvou.entrepreneur.prenom }}</td>
                    <td>{{ rendezvou.objet }}</td>
                    <td>{{ rendezvou.etat ? 'Confirmé' : 'Non confirmé' }}</td>
                    <td>{{ rendezvou.observation }}</td>

                </tr>
                {% else %}
                <tr>
                    <td colspan="8">Aucun enregistrement trouvé</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        <div class="navigation d-flex justify-content-center">
            {{ knp_pagination_render(rendezvous) }}
        </div>
    </div>
</section>
{% endblock %}