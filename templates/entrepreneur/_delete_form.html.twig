<br><br><br><br>
<form method="post" action="{{ path('entrepreneur_delete', {'id': entrepreneur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entrepreneur.id) }}">
    <button class="btn btn-danger">Supprimer le compte</button>
</form>
